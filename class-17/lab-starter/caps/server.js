/*
Responsibilities:
- server should listen for 'connection' events
- server should maintain a pool of sockets
- Each socket should listen for 'data' and 'close' events
- Should dispatch events to each socket
See Lab spec for details.
*/

